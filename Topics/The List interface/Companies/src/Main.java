import java.util.*;

class Main {
    public static void main(String[] args) {
        // put your code here
        Scanner scanner = new Scanner(System.in);
        List<String> yourList = Arrays.asList(scanner.nextLine().split(" "));
        System.out.println(yourList);
    }
}